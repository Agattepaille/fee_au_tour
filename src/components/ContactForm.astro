---
// ContactForm.astro
---
<div class="min-h-screen bg-background-secondary flex items-center justify-center px-4 py-8">
    <div class="bg-white rounded-lg shadow-xl p-8 w-full max-w-lg transform transition duration-300 hover:shadow-2xl hover:-translate-y-1 hover:scale-[1.01]">
        <h2 class="text-2xl font-semibold text-center text-primary-darker mb-6">
            Me contacter
        </h2>
        <p class="text-center text-lg text-gray-600 mb-6">
            N'hésitez pas à m'écrire pour toute question, demande ou organisation d'atelier dans votre structure.
        </p>
        <form name="contact" method="POST" class="space-y-4">
            <input type="hidden" name="form-name" value="contact"/>

            <div>
                <label for="name" class="block text-sm font-medium text-gray-700">Nom</label>
                <input
                        type="text"
                        id="name"
                        name="name"
                        required
                        class="mt-1 block w-full rounded-md border border-gray-300 shadow-sm px-4 py-2 focus:ring focus:ring-primary focus:outline-none"
                />
            </div>

            <div>
                <label for="email" class="block text-sm font-medium text-gray-700">Email</label>
                <input
                        type="email"
                        id="email"
                        name="email"
                        required
                        class="mt-1 block w-full rounded-md border border-gray-300 shadow-sm px-4 py-2 focus:ring focus:ring-primary focus:outline-none"
                />
            </div>

            <div>
                <label for="message" class="block text-sm font-medium text-gray-700">Message</label>
                <textarea
                        id="message"
                        name="message"
                        required
                        class="mt-1 block w-full rounded-md border border-gray-300 shadow-sm px-4 py-2 focus:ring focus:ring-primary focus:outline-none min-h-[120px]"
                ></textarea>
            </div>

            <button
                    type="submit"
                    class="w-full bg-primary text-white font-medium py-2 px-4 rounded-md hover:bg-primary-hover transition cursor-pointer"
            >
                Envoyer
            </button>
        </form>
    </div>
</div>
